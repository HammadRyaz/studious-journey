
public class HumanPlayer extends Player {
    public HumanPlayer(String name, String symbol) {
        super(name, symbol);
    }

    @Override
    public int[] makeMove(GameBoard board) {
        // Human moves are handled by the UI, so this method returns null.
        return null;
    }
}
